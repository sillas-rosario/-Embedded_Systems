#include<stdio.h>
#include<stdlib.h>
double * alocavet(int t){
    double *s;
    int t= tam;
    
    s= (double *) malloc(c*sizeof(double));
    if (s==NULL)
        return NULL;
    return s;
}

